{
  "name": "the-growth-machine",
  "version": "0.1.0",
  "description": "The Growth Book's execution body. Runs any moment through a six-station pipeline, wave after wave.",
  "type": "module",
  "private": true,
  "bin": {
    "growth-machine": "./bin/growth-machine"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "cli": "tsx src/cli.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
